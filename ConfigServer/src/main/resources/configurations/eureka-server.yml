server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

  server:
    wait-time-in-ms-when-sync-empty: 0

# Optional but useful
management:
  endpoints:
    web:
      exposure:
        include: "*"